name: SPARXSTAR Quality Control

on:
  push:
    branches: ["main", "universe"]
  pull_request:
    branches: ["main", "universe"]

jobs:
  syntax-check:
    name: Nginx Syntax Validation
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install Nginx
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y nginx

      - name: Deploy SPARXSTAR Event Horizon Config
        run: |
          # 1. Copy Logic file to http context directory (000- prefix ensures it loads first)
          sudo cp conf.d/000-spx-horizon-logic.conf /etc/nginx/conf.d/

          # 2. Copy Cloudflare trust file to http context directory
          sudo cp conf.d/spx-cloudflare-trust.conf /etc/nginx/conf.d/

          # 3. Create snippets directory and copy Rules file
          sudo mkdir -p /etc/nginx/snippets
          sudo cp snippets/spx-horizon-rules.conf /etc/nginx/snippets/

          # 4. Deploy the shared CI server block (defines varnish_upstream stub)
          sudo cp tests/nginx-test-server.conf /etc/nginx/sites-available/default

          # Ensure the default site is enabled
          sudo ln -sf /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default

      - name: Validate Nginx Syntax
        run: sudo nginx -t

  firewall-test:
    name: Attack Simulation
    runs-on: ubuntu-latest
    needs: syntax-check
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install Nginx
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y nginx

      - name: Deploy SPARXSTAR Event Horizon Config
        run: |
          sudo cp conf.d/000-spx-horizon-logic.conf /etc/nginx/conf.d/
          sudo cp conf.d/spx-cloudflare-trust.conf /etc/nginx/conf.d/
          sudo mkdir -p /etc/nginx/snippets
          sudo cp snippets/spx-horizon-rules.conf /etc/nginx/snippets/

          sudo cp tests/nginx-test-server.conf /etc/nginx/sites-available/default
          sudo ln -sf /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default

      - name: Validate & Start Nginx
        run: |
          sudo nginx -t
          sudo systemctl restart nginx

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Test Dependencies
        run: pip install requests pytest

      - name: Run Attack Simulation Tests
        run: pytest tests/test_firewall.py -v
